## c is a Numpy Array of any size > 3 dimensions and the function will slice the first three components and create a 3D interactive visual ##


def PCA_VIZ(c):

    x, y, z = c[0], c[1], c[2]

    fig = go.Figure(data=[go.Scatter3d(x=x, y=y, z=z,
                                   mode='markers')])

    fig.update_layout(width = 900,height= 1000)
    

    fig.show()
